[pytest]
# Тесты только в client и server; scripts/ не собираем (там скрипты с импортом железа).
testpaths = client/tests server/tests
python_files = test_*.py
markers =
    hardware: тесты на реальном оборудовании (камера, лидар, GPIO)
